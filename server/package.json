{
  "name": "dreamcord-server",
  "description": "",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "set-webhook": "wrangler secret put DISCORD_WEBHOOK_URL",
    "format": "biome check --write --unsafe ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.6",
    "wrangler": "4.49.0"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  }
}
